[project]
name = "orionradar"
version = "0.1.0"
description = "Live ADS-B tracking system and simulator with a web radar UI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "pandas",
    "pymodes>=3",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.16.0",
    "ty>=0.0.63",
]
